# Refactoring

Moved `crates/web` to `interfaces/web`.
Moved `crates/gui` to `interfaces/gui`.
Moved `crates/tui` to `interfaces/cli`.
Moved planning documents to `spec/`.
Created flutter project in `interfaces/mobile` for android & ios.
Created ffi crate in `crates/ffi`.
